import sys
import numpy as np

# required_edges = 100
# num = int(np.ceil(np.random.rand()**(1/64)*required_edges))
# idx = np.random.randint(0, num, 8)
# idx = np.sort(idx)
# print(idx, idx[1:6:2])

edge = (1, 2, 3, 4)

u, v = edge[:2]

print(u, v)